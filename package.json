{
    "name": "pkgame",
    "private": true,
    "scripts": {
        "build": "turbo build",
        "sandbox": "turbo dev --filter=sandbox",
        "dev": "turbo dev --filter=client --filter=server",
        "lint": "turbo lint"
    },
    "devDependencies": {
        "turbo": "^2.3.3",
        "typescript": "5.7.2"
    },
    "packageManager": "bun@1.1.42",
    "workspaces": [
        "frontend/*",
        "packages/config/*"
    ],
    "engines": {
        "node": ">=18"
    }
}