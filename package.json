{
    "name": "pkgame",
    "private": true,
    "scripts": {
        "build": "turbo build",
        "sandbox": "turbo dev --filter=sandbox",
        "dev": "turbo dev --filter=client --filter=server",
        "lint": "turbo lint"
    },
    "devDependencies": {
        "turbo": "^2.3.3",
        "typescript": "5.5.4"
    },
    "packageManager": "pnpm@9.15.3",
    "engines": {
        "node": ">=18"
    }
}