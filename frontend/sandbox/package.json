{
  "name": "@pkstudio/frontend-sandbox",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "tsc && bunx --bun vite build",
    "preview": "bunx --bun vite preview",
    "lint": "eslint \"src/**/*.ts\""
  },
  "devDependencies": {
    "@config/eslint": "workspace:*",
    "@config/typescript": "workspace:*",
    "@types/node": "^20",
    "typescript": "~5.6.2",
    "vite": "^6.0.5"
  }
}
